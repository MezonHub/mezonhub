syntax = "proto3";
package mezonhub.mezonhub.zvote;

import "gogoproto/gogo.proto";

// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/mezonhub/mezonhub/x/zvote/types";

// GenesisState defines the zvote module's genesis state.
message GenesisState {
    int64 proposal_life = 1 [(gogoproto.moretags) = "yaml:\"proposal_life\""];
    // this line is used by starport scaffolding # genesis/proto/state
}
